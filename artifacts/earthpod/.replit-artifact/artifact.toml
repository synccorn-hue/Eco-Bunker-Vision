kind = "web"
previewPath = "/"
title = "EarthPod™ Eco Bunker"
version = "1.0.0"
id = "artifacts/earthpod"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22278

[services.development]
run = "pnpm --filter @workspace/earthpod run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/earthpod", "run", "build" ]
publicDir = "artifacts/earthpod/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22278"
BASE_PATH = "/"
